.row
  .columns
    .small-12.columns.text-center
      %h1 All The Games
      - @games.each do |game|
        %p
          = link_to game_name(game), games_path(id: game.id)
        - if game.winner.nil?
          %p Claim your victory

        - elsif game.winner == current_player
          %p You won
        - else
          %p You lost

        = link_to 'Delete Game', games_path(id: game.id), method: :delete, data: {confirm: "Are you sure?"}

        %hr